def generate_queries(query, plan):

    return [
        query,
        f"{query} dataset paper",
        f"{query} benchmark comparison",
        f"{query} research survey",
        f"{query} strengths limitations"
    ]